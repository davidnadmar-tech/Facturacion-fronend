import { describe, it, expect } from 'vitest'

// Placeholder temporal mientras se migra la store de clientes a operaciones remotas
describe('clientes store (pendiente de migración)', () => {
  it('placeholder', () => {
    expect(true).toBe(true)
  })
})
